// import { createSelector } from "reselect";

// const serviceSelector = (state) => state.serviceSelector;

// export const selectNews = createSelector(
//   [serviceSelector],
//   (newsReducer) => newsReducer.services
// );
